import { Vector3, ArcRotateCamera, Scene, Engine } from "@babylonjs/core";

export default class PerspectiveCamera {
    constructor(aipha, beta, radius, scene){
        super()

        this.initCamera()
    }
}
